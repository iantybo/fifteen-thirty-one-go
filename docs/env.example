# Copy this file to `.env` in the repo root (do NOT commit `.env`) and adjust as needed.

# Backend
BACKEND_ADDR=127.0.0.1:8080
DATABASE_PATH=./backend/app.db
# IMPORTANT: never use "change-me" in production. Generate a strong secret, e.g.:
#   openssl rand -hex 32
JWT_SECRET=change-me
JWT_ISSUER=fifteen-thirty-one
JWT_TTL_MINUTES=10080

# App environment: development|staging|production
APP_ENV=development
# For production/staging WebSocket origin checking (comma-separated list of allowed Origins)
# WS_ALLOWED_ORIGINS=https://your-frontend.example.com,https://www.your-frontend.example.com
WS_ALLOWED_ORIGINS=

# Frontend (Vite)
VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_WS_BASE_URL=ws://127.0.0.1:8080


